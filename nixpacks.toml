[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd server && npm ci"]

[phases.build]
cmds = ["echo 'Skipping TypeScript compilation - using ts-node in production'"]

[start]
cmd = "cd server && npx ts-node --transpile-only src/index.ts"
